# Root requirements for the Vercel Python serverless function (api/index.py).
# Slimmed to ONLY what backend code imports — the unused google-generativeai /
# grpcio / google-api libraries are intentionally excluded to keep the
# serverless bundle small and cold starts fast. Pins match backend/requirements.txt.
fastapi==0.135.3
pydantic==2.12.5
httpx==0.28.1
groq==1.1.2
slowapi==0.1.9
python-dotenv==1.2.2
